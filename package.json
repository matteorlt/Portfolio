{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio — frontend Vite / React (formulaires via EmailJS)",
  "scripts": {
    "dev": "npm run client",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build:prod": "cd client && npm run build:prod",
    "install-all": "cd client && npm install",
    "preview": "cd client && npm run preview"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "license": "MIT"
}
